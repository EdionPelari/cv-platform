apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cv-website-ingress
spec:
  rules:
  - host: cv.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cv-website-cv-app
            port:
              number: 80
